# Pinned for reproducible Railway deploys. Top-level deps:
#   fastapi, uvicorn[standard], httpx, pydantic
# To upgrade: edit, run `pip install -U -r requirements.txt && pip freeze > requirements.txt`
# in a clean venv, then re-add this header.
fastapi==0.128.0
uvicorn[standard]==0.40.0
httpx==0.28.1
pydantic==2.12.5

# Transitives (pinned to prevent silent upstream drift)
annotated-types==0.7.0
anyio==4.12.1
certifi==2026.1.4
click==8.3.1
h11==0.16.0
httpcore==1.0.9
httptools==0.7.1
idna==3.11
pydantic_core==2.41.5
python-dotenv==1.2.1
PyYAML==6.0.3
starlette==0.50.0
typing-inspection==0.4.2
typing_extensions==4.15.0
uvloop==0.22.1
watchfiles==1.1.1
websockets==16.0
